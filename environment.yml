name: yolo2016
channels:
  - conda-forge
dependencies:
  - python=3.11
  - pip>=23.0
  - pip:
      - ultralytics>=8.3.0,<9.0.0
      - opencv-python>=4.9.0
      - numpy>=1.24.0
      - pandas>=2.0.0
      - matplotlib>=3.7.0
      - pyyaml>=6.0
      - tqdm>=4.65.0
      - jupyter>=1.0.0
      - ipykernel>=6.25.0
# Uwaga: dla wariantu GPU zainstaluj CUDA-owego PyTorcha zgodnie z
# https://pytorch.org/get-started/locally/ po utworzeniu środowiska.
